```bash
sudo apt install rsync -y && ./crave devspace
```
```bash
cd derpfest14
```

```bash
crave run --no-patch "rm -rf scripts && git clone https://github.com/projectelixeroscar/scripts.git && chmod u+x scripts/elixeroscar.sh && ./scripts/elixeroscar.sh "
```
```bash
crave run --no-patch "rm -rf scripts && git clone https://github.com/projectelixeroscar/scripts.git && chmod u+x scripts/sync.sh && ./scripts/sync.sh "
```
